<Window x:Class="KittyFocus.Windows.WarnDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="猫爪按住程序" Height="230" Width="440"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        Topmost="True"
        Background="#FFFBF6F0"
        ShowInTaskbar="False">
    <Grid Margin="24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,8">
            <TextBlock Text="🐾" FontSize="32" Margin="0,0,10,0"/>
            <TextBlock Text="猫爪按住程序！" FontSize="20" FontWeight="Bold"
                       Foreground="#FF6B4226" VerticalAlignment="Center"/>
        </StackPanel>

        <TextBlock Grid.Row="1" Text="检测到黑名单程序正在运行：" FontSize="13"
                   Foreground="#FF8A6A52" HorizontalAlignment="Center" Margin="0,0,0,4"/>

        <Border Grid.Row="2" Background="#FFF0E6DA" CornerRadius="8" Padding="12,8" Margin="0,0,0,14"
                HorizontalAlignment="Center">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="⚠ " FontSize="16" Foreground="#FFCC6633"/>
                <TextBlock x:Name="ProcessNameText" FontSize="14" FontWeight="SemiBold"
                           Foreground="#FF4A3526" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="3" HorizontalAlignment="Center" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="倒计时：" FontSize="13" Foreground="#FF8A6A52" VerticalAlignment="Center"/>
                <TextBlock x:Name="CountdownText" FontSize="22" FontWeight="Bold"
                           Foreground="#FFCC6633" VerticalAlignment="Center" Margin="6,0,0,0"/>
                <TextBlock Text=" 秒" FontSize="13" Foreground="#FF8A6A52"
                           VerticalAlignment="Center" Margin="2,0,0,0"/>
            </StackPanel>
            <TextBlock x:Name="StatusHint" Text="请关闭违规程序以保护猫咪 ✨"
                       FontSize="12" Foreground="#FFB59B82" HorizontalAlignment="Center"
                       TextAlignment="Center" TextWrapping="Wrap" Margin="0,6,0,0"/>
        </StackPanel>
    </Grid>
</Window>
